from setuptools import setup, find_packages

setup(
    name='entrypy',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    author='numbundupbap',
    description='entrypy: 엔트리 파일을 파이썬에서 실행',
    python_requires='>=3.7',
)
